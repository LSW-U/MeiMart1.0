import type { RiderProfile } from '@/src/types/rider';

export async function getRiderProfile(): Promise<RiderProfile | null> {
  return null;
}

export async function updateRiderProfile(_profile: Partial<RiderProfile>) {
  return undefined;
}
